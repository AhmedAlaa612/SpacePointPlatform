"""Design mission kind (P7, Phase 2 Stage 7) — the CubeSat mission-design
workbench ported from Madar (`MISSIONS_REPORT.md` Ch.1). Six pure budget
calculators plus the RF link math; the schema and verifier wiring around
them live in `models/missions/design.py` and
`services/missions/verifiers/design.py`.
"""
