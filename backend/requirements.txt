# Web framework
fastapi
uvicorn[standard]

# Database (async)
sqlalchemy
asyncpg
alembic
psycopg2-binary  # sync driver used only by scripts/migrate_legacy.py to read the legacy DB

# Phone normalization (spine identity matching, V2 R1-3)
phonenumbers

# Background job queue (V2 R2-1) — ticket emails + bulk imports run here, not
# synchronously in the request
arq
redis

# Tests (bootstrapped in V2 R1-3 — no harness existed before; deferred full CI/monorepo
# test setup is still V2 C0-2, this is just enough to run `pytest` locally against a
# dedicated test database)
pytest
pytest-asyncio

# Validation / settings
pydantic[email]
pydantic-settings
email-validator

# Auth
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
cryptography  # also used directly by services/storage_local.py (Fernet)

# Uploads / multipart
python-multipart

# Config
python-dotenv

# Supabase Storage
supabase

# Email (async SMTP)
aiosmtplib

# Document generation
reportlab
docxtpl
jinja2
pillow
qrcode
python-docx
openpyxl
pypdf
cairosvg
arabic-reshaper
python-bidi

# LMS content ingest (LM1-9) — pulling the boss's Drive dump straight to the
# VPS instead of through the operator's slow upload.
gdown

# LMS content ingest, private-folder path (2026-08-09) — the Drive folder
# turned out to be shared with the operator's specific account, not
# "anyone with the link", so gdown (unauthenticated) can't reach it. OAuth
# authenticates as the operator's own account instead, using whatever
# access they already have — no re-sharing, no service account.
google-auth-oauthlib
google-api-python-client

# Stripe Checkout — LMS course purchases (Stage S, August Build Brief
# Branch 4). The [async] extra pulls httpx so every call can use the
# _async suffix (create_async/retrieve_async) instead of blocking the
# event loop.
stripe[async]
